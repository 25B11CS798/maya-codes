#include <stdio.h>

int main() {
    int A, B, sum;
    
    // Read two integers
    scanf("%d %d", &A, &B);
    
    // Calculate sum
    sum = A + B;
    
    // Print sum with an extra 1 at the end
    printf("%d1
", sum);
    
    return 0;
}
