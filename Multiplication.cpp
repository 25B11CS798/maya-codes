#include <stdio.h>

int main() {
    int num1, num2;
    
    // Read two numbers
    scanf("%d %d", &num1, &num2);
    
    // Calculate multiplication
    int result = num1 * num2;
    
    // Print the result
    printf("%d
", result);
    
    return 0;
}
